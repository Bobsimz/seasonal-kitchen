package com.seasonaldining.recommendation.repository; import com.seasonaldining.recommendation.entity.RecommendationSession; import org.springframework.data.jpa.repository.JpaRepository; import java.util.Optional; public interface RecommendationSessionRepository extends JpaRepository<RecommendationSession,Long>{Optional<RecommendationSession> findByIdAndUserId(Long id,Long userId);}
