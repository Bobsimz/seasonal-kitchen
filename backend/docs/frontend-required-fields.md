# Frontend Required Fields

Inspection target: `../frontend`

## Field Gap Matrix

| Screen name | API endpoint | Required response DTO fields | Existing response fields if known | Missing response fields | Notes for frontend integration |
| --- | --- | --- | --- | --- | --- |
| Home main | `GET /api/v1/home` | `seasonTitle`, `seasonSubtitle`, `hero`, `ingredients`, `recipes`, `reels`, `trendingKeywords`, `unreadNotificationCount` | All listed fields exist | `weeklySeason`, dedicated `cta` object if required by future navigation contract | Current response can render the visible home sections. |
| Home ingredient cards | `GET /api/v1/home` or `GET /api/v1/ingredients` | `id`, `name`, `imageUrl`, `category`, `displayUnit`, `price.currentPrice`, `price.unit`, `price.weekChangeRate`, `trendDirection`, `priceChangeLabel`, `seasonal`, `badgeText`, `tags` | `id`, `name`, `imageUrl`, `category`, `price`, `seasonal`, `buyingSignal`, `tags` | `displayUnit`, `trendDirection`, `priceChangeLabel`, `badgeText`, rank/order | Frontend currently shows formatted strings; backend should return numeric values plus display labels where business-defined. |
| Home recipe/reels cards | `GET /api/v1/home`, `GET /api/v1/recipes`, `GET /api/v1/reels` | `id`, `title`, `thumbnailUrl`, `durationSeconds`, `viewCount`, `likeCount`, `creatorName`, `tags`, `seasonal` | Recipe/reel card fields exist where relevant | `reelCount` on recipe list if desired | Home supports mixed recipe/reels content. |
| Search result | `GET /api/v1/search?q={q}&type=ALL` | grouped `ingredients`, `recipes`, `reels` with counts plus compatible flat `items` | Grouped fields and counts exist | Advanced ranking/rankDelta | Current response supports screen sections. |
| Ingredient list | `GET /api/v1/ingredients` | `items`, `page`, `size`, `totalElements`, `hasNext`, card fields | ListResponse plus `IngredientCardResponse` | category filter metadata, display labels | Existing API mostly covers list but demo data and labels are needed. |
| Ingredient detail | `GET /api/v1/ingredients/{ingredientId}` | `id`, `name`, `category`, `imageUrl`, `baseUnit`, `seasonal`, `seasonScore`, `seasonMonths`, `price`, `buyingSignal`, `description`, `nutrition`, `careTips`, `storageTips`, `compareStoreCount` | All listed fields exist | Real seasonal calendar data may be empty/null until season integration | Current detail can render nutrition/care/storage cards. |
| Ingredient price history | `GET /api/v1/ingredients/{ingredientId}/prices` | `ingredientId`, `ingredientName`, `unit`, `source`, `items.observedDate`, `items.price`, optional `minPrice`, `maxPrice`, `changeRate` | documented existing price history fields | optional chart summary | Existing endpoint covers the basic chart. |
| Ingredient substitutes | `GET /api/v1/ingredients/{ingredientId}/substitutes` | `ingredientId`, `name`, `imageUrl`, `reason`, `score`, `price.currentPrice`, `unit`, `priceDeltaLabel` | Substitute DTO exists but exact fields should be verified during implementation | `price`, `unit`, `priceDeltaLabel` if absent | UI shows price and delta for each substitute. |
| Store offers / price compare | `GET /api/v1/ingredients/{ingredientId}/offers` | `storeId`, `storeName`, `storeType`, `logoUrl`, `logoText`, `brandColor`, `deliveryLabel`, `price`, `priceRangeMin`, `priceRangeMax`, `originalPrice`, `discountRate`, `badge`, `productUrl`, `observedAt` | All listed fields exist | `alertTargetPrice` belongs to user price-alert state | Required for price comparison screen and alert CTA. |
| Recipe list | `GET /api/v1/recipes` | `items`, `page`, `size`, `totalElements`, `hasNext`, `id`, `title`, `description`, `imageUrl`, `difficulty`, `minutes`, `servings`, `likeCount`, `viewCount`, `tags`, `seasonal`, `creatorName` | Listed fields exist | `reelCount` if desired | Visible list uses likes and category chips. |
| Recipe detail | `GET /api/v1/recipes/{recipeId}` | `id`, `title`, `description`, `imageUrl`, `difficulty`, `minutes`, `servings`, `ingredients`, `estimatedTotal`, `tags`, `creatorName`, `likeCount`, `relatedReels` | Listed fields exist | Optional values can be null for non-demo data | Required for recipe detail from reels flow. |
| Recipe steps | `GET /api/v1/recipes/{recipeId}/steps` | `stepNumber`, `description`, `minutes`, `imageUrl`, `tip` | `RecipeStepResponse` exists | `tip` if absent | Current UI can use existing step fields with optional tips. |
| Reels feed | `GET /api/v1/reels` | `items`, `id`, `recipeId`, `creatorId`, `creatorName`, `creatorAvatarUrl`, `videoUrl`, `thumbnailUrl`, `title`, `description`, `ingredientTags`, `likeCount`, `commentCount`, `saveCount`, `viewCount`, `durationSeconds`, `liked`, `saved`, `publishedAt` | Listed fields exist | `saved` defaults false until save domain exists | Required for visible reels feed. |
| Reel detail/actions | `GET /api/v1/reels/{reelId}`, `POST/DELETE /likes`, `GET/POST /comments`, `POST /view-events` | detail DTO, like state, comment list, write response, event response | Implemented | None blocking | Needed if frontend interactions are wired. |
| AI chat | `POST /api/v1/recommendations/plans`, `POST /api/v1/recommendations/plans/{planId}/messages` | `sessionId`, `planId`, `messages`, `assistantContent`, `suggestedActions`, `quickPrompts`, `status` | `ShoppingPlanResponse` includes session/plan/status/messages/quickPrompts; message response has `messageId`, `role`, `content` | `suggestedActions`, rich assistant card payload | UI is GPT-style chat with cards; real LLM rich cards are deferred. |
| AI result / shopping plan | `GET /api/v1/shopping-plans/{planId}` | `planId`, `summary`, `estimatedTotal`, `expectedSavingRate`, `meals`, `items`, `reasons`, `substitutions`, `selectedCount` | Typed plan fields exist; store groups are separate endpoint | `selectedCount` can be derived client-side | Items are typed DTOs. |
| Store split checkout | `GET /api/v1/shopping-plans/{planId}/store-links` | `savingAmount`, `storeGroups.storeName`, `storeType`, `deliveryLabel`, `totalPrice`, `items`, `externalCheckoutUrl` | Implemented | Real checkout completion is deferred | Needed for checkout screen even though not currently mounted in main artboards. |
| My page | `GET /api/v1/users/me/summary` plus existing user APIs | `profile`, `stats`, `preferences`, `activeAlertCount`, `favoriteCount`, `pantryCount`, `monthlySaving`, `personalizedIngredients`, `menuRows` | Aggregate endpoint exists | Real order/saving calculation is deferred | Aggregated endpoint prevents many calls and aligns with visible screen. |
| Notifications | `GET /api/v1/notifications` | `items[].id`, `type`, `category`, `title`, `body`, `subtitle`, `icon`, `severity`, `readAt`, `createdAt`, `relativeTime`, `actionTargetType`, `actionTargetId`, `tabCounts` | Implemented as `data.items + data.tabCounts` | Date group labels are client-derived | Response shape changed from prior plain list. |
| Survey preferences | `PUT /api/v1/users/me/preferences` | request: `householdSize`, `budget`, `spicyAvoid`, `priority`, `allergyCodes` | Preference response lacks allergy list | `allergyCodes` in preference update/response or separate allergies endpoint | Allergies are visible in signup survey and need persistence. |

## DTO Enrichment Principles

- Keep numeric values numeric; frontend can format currency and large counts.
- For business labels such as buying signal, trend direction, notification category, and alert severity, backend should return stable enum/string fields.
- For purely presentational icons/colors, frontend can map from semantic fields unless demo parity requires examples.
- All list APIs should continue using the common `ListResponse<T>` shape.
